#!/bin/bash

# Settings to load after loading virtualenv

if [[ $(current_branch) == 'develop' ]]; then
    SETTING='settings_dev'
else
    SETTING='settings'
fi

export DJANGO_SETTINGS_MODULE="lukexor_me.${SETTING}"

cdproject

if [[ ! $TERM =~ screen ]]; then
    screen -s lukexor_me -d -R
fi
